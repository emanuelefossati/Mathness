- in INode also store the index of the reference token
- refactor error_t type into a struct in order to also store the index of the reference token  
- implement a way to switch between rad and degree input format in trigonometric functions
- implement a way to represent infinity as a scalar_t value (if not already implemented by c++)